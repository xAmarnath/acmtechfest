# Flask and core dependencies
Flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.5.0

# Database
pymongo==4.6.0

# Security (no additional packages needed, using stdlib)
# secrets - built-in
# hashlib - built-in

# Production server (recommended)
gunicorn==21.2.0

# Optional: Better password hashing
# Uncomment for production use
# bcrypt==4.1.2
# argon2-cffi==23.1.0
